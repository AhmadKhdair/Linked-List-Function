int isLast(struct node* P, struct node* L) {
    return (P != NULL && P->Next == NULL); // if the condition is (P->Next == NULL && P != NULL && ) there will be a big problem when P == Null , cuz i want P->Next but its already Null ! so dont forget it ahmad
}
